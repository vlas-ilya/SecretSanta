import './styles.scss';

import { ReactNode } from 'react';
import React from 'react';

export type EditableProps = {
  view: ReactNode;
  edit: ReactNode;
}

export const Editable = () => (
  <div></div>
)